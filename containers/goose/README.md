# Goose runtime

This directory is reserved for the next implementation phase. The future image
will run as a non-root user, receive only explicit project mounts and will not
receive the Docker socket.
