# Contributing

Contributions are developed inside the WSL Linux filesystem. Installer changes must stay modular, rerunnable and non-destructive; version changes stay aligned with documentation and tests, and `./tools/release-check.sh` is the pre-commit check. The maintained contribution rules are in [Project Context](CONTEXT.md).
